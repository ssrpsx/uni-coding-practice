#ifndef BFS_H
#define BFS_H

#include <bits/stdc++.h>
using namespace std;

template<typename T>
class BFS{
    vector<vector<T>> graph;
    set<T> visited;

    private:
        void bfs(T start){

        }
}

#endif